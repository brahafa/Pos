<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pos.bringit">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/bringit_logo"
        android:label="${appName}"
        android:name=".POSApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SplashActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".activities.CreateOrderActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".activities.WorkersClockActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".activities.SearchOrderActivity"
            android:screenOrientation="landscape" />
    </application>

</manifest>